
  # Build as per file

  This is a code bundle for Build as per file. The original project is available at https://www.figma.com/design/52Yuq1SNH15hKwZxvBOsyg/Build-as-per-file.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  